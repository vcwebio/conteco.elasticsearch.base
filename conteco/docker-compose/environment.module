#!/usr/bin/env bash

# START CONFIGURATION
export CONTECO_DC_GLOBAL_STACKS=''elasticsearch.base''
# END CONFIGURATION

# START OTHER VARIABLES
# END OTHER VARIABLES

# START VOLUMES VARIABLES
export CONTECO_DC_ELASTICSEARCH_VOLUMES_NAME="${CONTECO_ECOSYSTEM}_elasticsearch_volume"
export CONTECO_DC_GLOBAL_VOLUMES="${CONTECO_ECOSYSTEM}_elasticsearch_volume: { }"
# END VOLUMES VARIABLES

# START PORTS VARIABLES
export CONTECO_DC_ELASTICSEARCH_PORTS="9200:9200"
# END PORTS VARIABLES

# START NETWORKS VARIABLES
export CONTECO_DC_ELASTICSEARCH_NETWORKS="${CONTECO_ECOSYSTEM}_overlay"
export CONTECO_DC_GLOBAL_NETWORKS="${CONTECO_ECOSYSTEM}_overlay: { external: true }"
# END NETWORKS VARIABLES

# START DEPLOY VARIABLES
export CONTECO_DC_ELASTICSEARCH_DEPLOY_LABELS="traefik.enable=true,traefik.http.services.${CONTECO_TYPE}.loadbalancer.server.port=9200,traefik.http.routers.${CONTECO_TYPE}.rule=PathPrefix(\"/${CONTECO_ECOSYSTEM}/${CONTECO_TYPE}\")"
export CONTECO_DC_ELASTICSEARCH_DEPLOY_RESOURCES_LIMITS_CPUS="0.75"
export CONTECO_DC_ELASTICSEARCH_DEPLOY_RESOURCES_RESERVATIONS_CPUS="0.1"
export CONTECO_DC_ELASTICSEARCH_DEPLOY_RESOURCES_LIMITS_MEMORY="384m"
export CONTECO_DC_ELASTICSEARCH_DEPLOY_RESOURCES_RESERVATIONS_MEMORY="256m"
# END DEPLOY VARIABLES


